// pre-defined data struct
// 

#include <array>
#include <cstddef>

using std::size_t;

typedef size_t vertex_t;
typedef int value_t;

typedef struct Edge {
    value_t u;    // capacity
    value_t b;     //cost
    vertex_t source_num;
    vertex_t sink_num; 

} ffsda;


typedef struct Vector {
    vertex_t vertex_num;
    value_t returnSupplyDemand() {
        return v;
    };
    private :

    value_t v;      // supply and demand 

} ggasxg;

typedef struct Graph {
    const int vertex_count;
    Graph (int ver_) 
        : vertex_count(ver_) {
            // 
        }
} sadhzyq;
